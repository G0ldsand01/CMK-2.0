---
import Layout from '@layouts/Layout.astro';
import { LogoutPage } from '@/components/LogoutPage';

const user = Astro.locals.user;

if (!user) {
	return Astro.redirect('/');
}
---
<Layout title="Logout" navbar={false}>
	<LogoutPage client:load/>
</Layout>
